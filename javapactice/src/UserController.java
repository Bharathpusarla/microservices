import java.util.Comparator;
import java.util.stream.Collectors;

public static void main(String[] args) {



List<String> fruits = Arrays.asList("Banana","Apple","mango","apple");

fruits.stream().filter(i -> i.startWith([^aA])).collect(Collectors.toList()))
fruits.stream().so
numbers = [15,12,90,1,19,54]

for(int i : numbers){
    for(int j=0,j<=numbers.length()-1;j++){
         if(i< numbers[j]){
             temp=i;
             i=j;
             j=i;
        }
    }
}
public int add( int a, int b){
    return a+b;
}
public int add(int a,)
    str1="bharath"
    str ="";
  for(int i=str1.lenhth();i>=0;i--){
      ststr1.charAt(i)
        }
  str="bharath" ;
 for(char c:str.toCharaarray()){
     for(int i=0;i<=str.length;i++){
         int count=0;
           if(c==str.charAt(i)){
               count++
        }

        }

        }
 Select Max(Salary) from employee where salary <( Select Max(Salary) from employee where salary)
}